<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="description" content="">
    <meta name="keywords" content="">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta http-equiv="Cache-Control" content="no-siteapp" />
    <title><%= title %> — 计研167评分系统</title>
    <link rel="icon" type="image/png" href="/i/favicon.png"/>
    <link rel="stylesheet" href="/css/amazeui.min.css"/>
    <link href="/pnotify/css/pnotify.css" rel="stylesheet" type="text/css">
    <link href="/pnotify/css/pnotify.brighttheme.css" rel="stylesheet" type="text/css">

    <link rel='stylesheet' href='/css/app.css' />
    <link rel="stylesheet" href="/css/style.css"/>
</head>

<body class="theme-white">
    <div class="flex-container">
        <div class="tpl-login">
            <div class="tpl-login-content">
                <h1><%= title %></h1>
                <form class="am-form tpl-form-line-form" method="POST" id="score-form" onSubmit="return false;" >
                    <div class="am-form-group">
                        <label for="uid" class="am-u-sm-4 am-form-label am-padding-horizontal-0">选择对象：</label>
                        <div class="am-u-sm-8" style="text-align: left;">
                            <select id="uid" name="uid" data-am-selected="{searchBox: 1, maxHeight: 200}" required>
                                <option value=""></option>
                                <% users.forEach(function(user) { %>
                                    <option value="<%= user.id %>"><%= user.username %></option>
                                <% }) %>
                            </select>
                        </div>
                    </div>
                    <div class="am-form-group">
                        <label for="score" class="am-u-sm-4 am-form-label am-padding-horizontal-0">选择分值：</label>
                        <div class="am-u-sm-8" style="text-align: left;">
                            <select id="score" name="score" data-am-selected="{maxHeight: 200}" required>
                                <% for(var i = 100; i >= 0; i = i-5) { %>
                                    <option value="<%= i %>"><%= i + ' 分 ' %></option>
                                <% } %>
                            </select>
                        </div>
                    </div>
                    <div class="am-form-group">
                        <div class="am-u-md-12">
                            <button type="submit" class="am-btn am-btn-primary  am-btn-block tpl-btn-bg-color-success tpl-login-btn" id="btn-score">打分</button>
                        </div>
                    </div>
                </form>
                <% if (message){ %>
                    <p class="am-text-danger am-text-xs"><%= message %></p>
                <% } %>
            </div>
        </div>
        <div class="footer">
            &copy;2017 - <a href="https://helloarron.com">Arron</a> 计研167
        </div>
    </div>

    <!--JavaScript-->
    <script src="/js/jquery.min.js" type="text/javascript"></script>
    <script src="/js/socket.io/socket.io.min.js" type="text/javascript"></script>
    <script src="/js/amazeui.min.js" type="text/javascript"></script>
    <script src="/pnotify/js/pnotify.js" type="text/javascript"></script>
    <script src="/js/app.js" type="text/javascript"></script>
</body>

</html>